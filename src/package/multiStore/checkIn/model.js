import {
    checkIn,
    getRedPackage
} from "../../../service/api"
import {isFunction} from "../../../utils/utils";
export default {
    namespace: "checkIn",

    state: {

    },

    effects: {
        // *checkInAction({payload, callback}, {call, put}){
        //     const res = yield call(checkIn, payload);
        //     isFunction(callback) && callback(res);
        // },
        // *getRedPackageAction({payload, callback}, {call, put}){
        //     const res = yield call(getRedPackage, payload);
        //     isFunction(callback) && callback(res);
        // }
    },

    reducers: {

    },

};
